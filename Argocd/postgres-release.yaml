apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: postgresql
  namespace: argocd
  annotations:
    notifications.argoproj.io/subscribe.on-health-degraded.mattermost: ""
    notifications.argoproj.io/subscribe.on-sync-status-unknown.mattermost: ""
    notifications.argoproj.io/subscribe.on-sync-failed.mattermost: ""

spec:
  project: default
  destination:
    namespace: dev
    server: https://kubernetes.default.svc
  sources:
    - chart: postgresql
      repoURL: https://charts.bitnami.com/bitnami
      targetRevision: 18.1.3
      helm:
        valueFiles:
          - $values/Common/postgresql-values.yaml
    - repoURL: 'https://github.com/maheshgowdamg/Databases.git'
      targetRevision: HEAD
      ref: values
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
